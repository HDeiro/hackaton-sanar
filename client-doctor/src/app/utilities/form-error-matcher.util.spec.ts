import { FormErrorMatcher } from './form-error-matcher.util';

describe('FormErrorMatcher', () => {
  it('should create an instance', () => {
    expect(new FormErrorMatcher()).toBeTruthy();
  });
});
